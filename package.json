{
  "name": "esbuild-repro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild app.jsx --bundle --outfile=out.js --supported:const-and-let=true"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "0.17.19",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
